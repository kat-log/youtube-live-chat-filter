# 変更履歴

**利用者から見える変更だけ**を書く。内部の作り直しは「何が良くなったか」に翻訳して書き、
経緯そのものは [`docs/redesign-plan.md`](docs/redesign-plan.md) の実施記録に置く。

バージョンの正は [`src/manifest.json`](src/manifest.json) の `version`。
[`README.md`](README.md) の冒頭と**対で**更新する（過去に片方だけ古くなった。監査 #44）。

1.10.0 以前は git のコミット履歴を参照（このファイルは 2.0.0 から始めた）。

---

## 2.0.0 — 2026-09-08

10回のセッション（フェーズ0〜9）に分けて `src/` 全体を作り直した。
機能の追加は少ないが、**取り込み・保存・描画・ライフサイクルの仕組みが全部変わっている**ため
メジャーバージョンとした。46件の欠陥と6つの構造的な原因を片付けている
（[`docs/audit-2026-09.md`](docs/audit-2026-09.md) / [`docs/redesign-plan.md`](docs/redesign-plan.md)）。

### 要求する権限が減りました

- `activeTab` と `tabs`（**全タブのURLとタイトルを読む権限**）を**削除**。
  YouTube のタブのURLはホスト権限だけで読めるため、そもそも要らなかった
- ホスト権限を `*://*.youtube.com/*` → `https://*.youtube.com/*` に狭め（平文HTTPを外した）、
  `*://*.googleapis.com/*` → `https://www.googleapis.com/youtube/v3/*` に**APIのパスまで限定**
- 増えたのは `alarms`（1分ごとの見張り。**権限の警告は出ない種類**）だけ

権限は実質減る方向なので、更新時に拡張機能が無効化されることはありません。

### 直ったこと・良くなったこと

- **フィルターが過去のコメントにも効く。** 取り込みは全件になり、フィルターは表示側だけの操作に
  なった。あとからトグルをONにすれば、それまで隠れていたコメントも遡って出てくる
  （これまでは取り込み時にフィルターをかけていたため、OFFにしていた分は永久に失われていた）
- **数千件たまってもポップアップが固まらない。** 一覧は新着分だけを足す描画に変わり、
  検索の初回で全件を作り直すこともなくなった
- **Service Worker が終了してもコメントの取得が続く。** 状態の持ち方を1つに畳み、
  1分ごとの見張り（`chrome.alarms`）が止まった監視を拾い直す。
  **APIの割り当て（quota）エラーで止まったあとも、放置すれば自動で再開する**
- **YouTube の「上位のチャット ↔ チャット」を切り替えても止まらない。**
  チャット欄の中身が差し替わったことを検知して監視を張り直す
- **チャットを読み取れているかがトップバーに出る。** 読めていれば緑の点。
  「動いているのにコメントが増えない」のか「そもそも読めていない」のかが分かる
- **Tab キーだけでフィルターとダークモードを操作できる。** フォーカスの枠も見えるようにした。
  **設定画面もダークテーマに追従する**（これまではポップアップだけだった）
- **履歴の保存先が IndexedDB になった。** 書き込み量が桁で減り、
  コメントが増えるほど重くなる・容量を使い切って監視が止まる、が起きにくくなった。
  **これまでの履歴（直近5配信ぶん）は初回起動時に自動で引き継がれる**
- ポップアウトしたチャット窓で、スクリプトが二重に動くことがあったのを修正
- 動画を切り替えた直後に「自動停止しました」と出ることがあったのを修正

### 内部（利用者からは見えない）

- コメントの型・ID・検索用文字列を `src/shared/comment.js` に一本化（3つの実行環境から読む）
- 履歴の保存を `src/shared/store.js`（IndexedDB）に一本化。テーマの適用は `src/shared/theme.js`
- ポップアップと Service Worker の通信を `chrome.runtime.connect` のポートに変更
- ESLint と GitHub Actions の CI を導入。テストは 281 件（`npm test` / `npm run lint`）
- ビルド手順は無いまま。`src/` をそのまま読み込める性質は維持している

---

## 1.12.5 — 2026-09-06

- チャットからコピーした語に混ざる不可視文字（ゼロ幅スペースなど）で検索が0件になるのを修正

## 1.12.4 — 2026-09-06

- 検索が全角・半角の違いや末尾のスペースで取りこぼすのを修正

## 1.12.3 — 2026-09-05

- 全件スキャンでもスーパーステッカーの画像が揃うまで待つように修正

## 1.12.2 — 2026-09-05

- 新着のスーパーステッカーで画像とステッカー名が落ちるのを修正

## 1.12.1 — 2026-09-05

- 一般バッジのコントラストと、フィルターのON/OFF表示を改善

## 1.12.0 — 2026-09-05

- スーパーステッカーの画像そのものをポップアップに表示（DOMモードのみ）

## 1.11.0 — 2026-09-05

- スーパーチャット／スーパーステッカーと、メンバーシップ（新規加入・継続・ギフト購入）に対応。
  フィルターのトグルが4つから6つになった
